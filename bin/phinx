#!/usr/bin/env sh

# configure
set -e
set -u

# declare variables
curdir=$( cd "$( dirname "$0" )" && pwd )
rootdir="$curdir/../"

# script

# Include phinx secrets if present in app root
if [ -f  "$rootdir/phinx.secrets" ] ; then
    source "$rootdir/phinx.secrets"
fi

"$rootdir/vendor/bin/phinx" $@

