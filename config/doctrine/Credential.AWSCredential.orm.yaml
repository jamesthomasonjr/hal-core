'Hal\Core\Entity\Credential\AWSCredential':
    type: 'embeddable'

    fields:

        key:
            column: 'aws_key'
            type: 'string'
            length: 100
        secret:
            column: 'aws_secret'
            type: 'string'
